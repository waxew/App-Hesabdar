# Update Policy

## قرارداد ثابت نصب و آپدیت

- `applicationId = com.waxew.hesabdar` بعد از انتشار پایدار تغییر نمی‌کند.
- `versionCode` در هر نسخه جدید باید عددی بزرگ‌تر از نسخه قبلی باشد.
- `versionName` برای کاربر قابل خواندن است و بر اساس SemVer نگهداری می‌شود.
- APKهای Publish باید همیشه با همان Signing Key پایدار امضا شوند.

## حفظ اطلاعات

- تغییر Room Schema فقط با Migration صریح انجام می‌شود.
- `fallbackToDestructiveMigration` در نسخه Production ممنوع است.
- حذف/بازسازی دیتابیس برای رفع خطای Migration راه‌حل قابل قبول نیست.
- قبل از تغییر بزرگ Schema مسیر Upgrade از نسخه‌های قبلی بررسی می‌شود.
- اسناد مالی نهایی Hard Delete نمی‌شوند؛ Void/Reversal برای حفظ تاریخچه استفاده می‌شود.

## Backup قبل از تغییرات حساس

قبل از Restore یا عملیات حساس، Backup توصیه می‌شود. نسخه 1.0 از Backup محلی و Backup رمزگذاری‌شده HDBX پشتیبانی می‌کند.

## Signing Key

Signing Key خصوصی نباید در Repository عمومی قرار گیرد. GitHub Actions فقط از Secrets استفاده می‌کند. نسخه‌ای که با کلید متفاوت امضا شود Update مستقیم نسخه نصب‌شده قبلی نخواهد بود.

## سیاست نسخه‌ها

- Patch (`1.0.x`): رفع باگ و تغییرات سازگار.
- Minor (`1.x.0`): قابلیت جدید با حفظ داده و سازگاری نصب.
- Major (`x.0.0`): تغییرات معماری بزرگ؛ همچنان تا حد ممکن Upgrade داده و امضای ثابت حفظ می‌شود.
