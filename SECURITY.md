# Security Policy

## داده‌های مالی

هسته حسابدار Offline-First است و داده‌های مالی اصلی در Room/SQLite داخل فضای برنامه نگهداری می‌شوند. عملیات چندجدولی حساس داخل Transaction انجام می‌شوند تا در خطا نیمه‌کاره باقی نمانند.

## PIN محلی

PIN به‌صورت متن ساده ذخیره نمی‌شود. لایه امنیتی برنامه از مشتق‌سازی رمز و Salt برای اعتبارسنجی PIN استفاده می‌کند.

## Backup

دو نوع Backup وجود دارد:

1. Backup محلی `.hdb` برای بازیابی سریع روی همان دستگاه/فضای برنامه.
2. Backup قابل‌انتقال `.hdbx` با رمز کاربر.

HDBX از PBKDF2-HMAC-SHA256 برای مشتق‌سازی کلید و AES-GCM برای رمزگذاری و کنترل صحت استفاده می‌کند. کلید داخل فایل ذخیره نمی‌شود و به Android Keystore وابسته نیست؛ بنابراین انتقال به دستگاه جدید با همان رمز ممکن است.

قبل از Restore رمزگذاری‌شده، برنامه یک Backup اضطراری از دیتابیس فعلی می‌سازد. فایل پس از رمزگشایی باید Header معتبر SQLite داشته باشد.

## Signing

Keystore انتشار، رمز Keystore، Alias و رمز Key هرگز داخل Repository عمومی Commit نمی‌شوند. Build Release فقط از این GitHub Secrets استفاده می‌کند:

- `HESABDAR_RELEASE_KEYSTORE_BASE64`
- `HESABDAR_RELEASE_KEYSTORE_PASSWORD`
- `HESABDAR_RELEASE_KEY_ALIAS`
- `HESABDAR_RELEASE_KEY_PASSWORD`

از دست دادن Keystore پایدار انتشار به معنی از دست دادن امکان ارائه APK آپدیت‌شونده با همان امضا است. از Keystore باید خارج از Repository حداقل دو نسخه پشتیبان امن وجود داشته باشد.

## گزارش آسیب‌پذیری

اطلاعات حساس، رمز، Keystore یا داده واقعی مشتریان را داخل Issue عمومی GitHub منتشر نکنید. برای ارتباط پشتیبانی از اطلاعات تماس داخل خود برنامه استفاده شود.
