# Changelog

تمام تغییرات مهم پروژه حسابدار در این فایل ثبت می‌شوند.

## 0.11.0 Beta

- افزایش `versionCode` به 6 و `versionName` به `0.11.0`
- ارتقای Room Schema از نسخه 4 به 5 با Migration صریح و بدون حذف داده
- اضافه شدن شماره سند پایدار و یکتا برای فروش، خرید و مرجوعی‌ها
- اضافه شدن جدول `document_sequences` برای شمارنده تراکنشی شماره اسناد
- تبدیل اسناد قدیمی به شماره‌های پایدار در Migration نسخه 5
- اضافه شدن وضعیت‌های `POSTED`، `VOIDED` و `REVERSAL`
- اضافه شدن ابطال حرفه‌ای فروش و خرید با سند معکوس به‌جای حذف یا ویرایش تاریخچه
- برگشت خودکار اثر ابطال روی موجودی، دریافت/پرداخت و سند حسابداری دوبل
- جلوگیری از ابطال خرید در صورتی که موجودی لازم برای برگشت از انبار وجود نداشته باشد
- ثبت اجباری علت ابطال و ذخیره آن در Audit Trail
- اضافه شدن `reversesInvoiceId` برای اتصال سند معکوس به سند اصلی
- اضافه شدن `voidedAt` و `voidReason`
- اضافه شدن Snapshot بهای واحد (`unitCost`) به هر ردیف فاکتور
- استفاده از بهای تاریخی ردیف به‌جای قیمت خرید فعلی در گزارش سود
- استفاده از Snapshot بهای تاریخی برای معکوس‌کردن COGS هنگام ابطال فروش
- جلوگیری از تغییر نوع «کالا/خدمت» پس از ثبت برای حفظ صحت کارتکس و تاریخچه مالی
- اضافه شدن شماره سند و وضعیت چرخه عمر به CSV و PDF فاکتورها
- نمایش وضعیت سند و علت ابطال در رابط فاکتور
- گرفتن تایید و علت قبل از ابطال عملیات مالی
- اضافه شدن Unit Test برای فرمت پایدار شماره اسناد
- صفحه درباره نرم‌افزار نسخه واقعی Build را از `BuildConfig.VERSION_NAME` می‌خواند

### محدودیت‌های باقی‌مانده

- Draft واقعی و امکان ذخیره پیش‌نویس فاکتور هنوز اضافه نشده است.
- ویرایش سند POSTED عمداً آزاد نیست؛ در ادامه جریان «اصلاح با Reversal + Repost» تکمیل می‌شود.
- بهای تاریخی از Snapshot قیمت خرید زمان ثبت استفاده می‌کند؛ موتور میانگین موزون/FIFO کامل هنوز مرحله بعد است.
- اسکن بارکد دوربین، واحدهای چندگانه، Backup رمزگذاری‌شده و Release امضاشده هنوز باقی مانده‌اند.

## 0.10.0 Beta

- افزایش `versionCode` به 5 و `versionName` به `0.10.0`
- ارتقای دیتابیس Room از نسخه 3 به 4 با Migration صریح و بدون حذف داده‌های قبلی
- اضافه شدن کد کالا / SKU، بارکد، دسته‌بندی، واحد، حداقل موجودی و نوع کالا/خدمت
- اضافه شدن جستجوی کالا بر اساس نام، کد، بارکد و دسته‌بندی
- اضافه شدن هشدار کمبود موجودی در صفحه کالا و داشبورد
- پشتیبانی از «خدمت» بدون ایجاد گردش مصنوعی انبار
- اضافه شدن ویرایش مشخصات کالا/خدمت با Audit Trail
- اضافه شدن اصلاح دستی موجودی افزایشی/کاهشی با علت، کارتکس و Audit Trail
- جلوگیری از منفی‌شدن موجودی در اصلاح دستی
- اضافه شدن تخفیف مبلغی سطح فاکتور
- اضافه شدن درصد مالیات با محاسبه بعد از تخفیف
- اضافه شدن هزینه حمل/ارسال
- ذخیره جداگانه جمع اولیه، تخفیف، مالیات، حمل و مبلغ نهایی فاکتور
- ارتقای PDF فاکتور برای نمایش جزئیات تخفیف، مالیات و حمل
- ارتقای CSV فاکتورها برای خروجی جزئیات کامل مبلغ
- ارتقای گزارش PDF خلاصه برای نمایش مالیات فروش و خرید
- اضافه شدن حساب سیستمی مالیات خرید، مالیات فروش، درآمد حمل و خدمات خریداری‌شده
- تفکیک ثبت دوبل مالیات و حمل از فروش/خرید اصلی
- تفکیک خدمات خریداری‌شده از موجودی کالا در سند حسابداری
- ادامه استفاده از `Long` برای مبالغ و کنترل Overflow در محاسبات حساس
- اضافه شدن Unit Test برای ترتیب محاسبه تخفیف، مالیات و حمل

## 0.9.0 Beta

- افزایش `versionCode` به 4 و `versionName` به `0.9.0`
- توسعه رابط اصلی به ساختار کامل‌تر با Drawer راست‌به‌چپ، پروفایل محلی، Bottom Navigation، ارتباط با ما و درباره نرم‌افزار
- ذخیره تصویر پروفایل در فضای خصوصی برنامه
- اضافه شدن تاریخ شمسی داخلی بدون وابستگی به سرویس اینترنتی
- اضافه شدن تنظیمات مشخصات کسب‌وکار، واحد پول، عنوان سال مالی و پیشوند فاکتور
- اضافه شدن تنظیمات زیرساخت ظاهر برای حالت System/Light/Dark
- فاکتور چندردیفی برای فروش، خرید، برگشت از فروش و برگشت از خرید
- ثبت Atomic فاکتور، ردیف‌ها، موجودی، تسویه، Audit و سند حسابداری
- اضافه شدن PDF اختصاصی هر فاکتور با اطلاعات کسب‌وکار، ردیف‌ها، طرف حساب، تاریخ شمسی، جمع، تسویه و مانده
- اضافه شدن CSV سازگارتر با Excel با UTF-8 BOM
- اضافه شدن PDF خلاصه مدیریتی
- اضافه شدن کارتکس کالا در رابط کاربری
- اضافه شدن پروفایل مالی طرف‌حساب و صورت‌حساب زمانی
- اضافه شدن کدینگ پیش‌فرض حسابداری
- اضافه شدن اسناد خودکار دوبل برای فروش، خرید، مرجوعی، درآمد، هزینه، دریافت و پرداخت
- اصلاح سند برگشت از خرید برای کاهش مستقیم حساب موجودی کالا
- اضافه شدن سند حسابداری دستی با کنترل توازن بدهکار/بستانکار
- اضافه شدن تراز آزمایشی، کنترل توازن دفتر و سود برآوردی کالاها
- اضافه شدن صندوق و حساب بانکی محلی
- اضافه شدن درآمد، هزینه، دریافت و پرداخت مستقل
- اضافه شدن چک دریافتی و پرداختی و مدیریت وضعیت آن
- اضافه شدن اقساط و پرداخت کامل/ناقص اقساط
- اضافه شدن یادآوری آفلاین چک‌ها و اقساط با AlarmManager و Notification
- اضافه شدن PIN محلی با PBKDF2-HMAC-SHA256 و Salt تصادفی؛ PIN خام ذخیره نمی‌شود
- اضافه شدن Backup کامل فایل SQLite پس از WAL checkpoint و Restore محلی
- اضافه شدن Audit Trail برای عملیات حساس
- اضافه شدن Unit Test برای تبدیل تاریخ شمسی
- CI قبل از ساخت APK تست Unit را اجرا می‌کند

## 0.3.0

- افزایش `versionCode` از 2 به 3
- افزایش `versionName` از `0.2.0` به `0.3.0`
- ارتقای دیتابیس Room از نسخه 2 به 3 با Migration صریح و بدون حذف داده‌های قبلی
- اضافه شدن صندوق و حساب بانکی (`treasury_accounts`)
- اضافه شدن ثبت درآمد و هزینه و گردش خزانه (`cash_entries`)
- اضافه شدن دریافت و پرداخت مستقل از فاکتور
- اضافه شدن کدینگ حسابداری (`ledger_accounts`)
- اضافه شدن سربرگ سند و آرتیکل‌های حسابداری (`journal_documents`, `journal_lines`)
- کنترل توازن بدهکار و بستانکار هنگام ثبت سند دوبل
- اضافه شدن چک دریافتی و پرداختی
- اضافه شدن اقساط
- اضافه شدن Audit Trail
- اضافه شدن گزارش‌های اولیه

## 0.2.0

- افزایش `versionCode` از 1 به 2
- افزایش `versionName` از `0.1.0` به `0.2.0`
- ارتقای دیتابیس Room از نسخه 1 به 2 با Migration واقعی و بدون حذف داده‌های قبلی
- اضافه شدن فاکتور، ردیف فاکتور، پرداخت و گردش انبار
- اضافه شدن `AccountingRepository` برای ثبت Atomic خرید و فروش
- کاهش/افزایش خودکار موجودی در فروش و خرید
- ثبت دریافت/پرداخت اولیه همزمان با فاکتور
- داشبورد واقعی فروش، خرید، مطالبات و بدهی

## 0.1.0

- ایجاد اسکلت اولیه اندروید
- Kotlin + Jetpack Compose + Material 3
- Room + SQLite محلی
- رابط فارسی و RTL
- اشخاص و طرف‌حساب‌ها
- کالاها
- داشبورد اولیه
- GitHub Actions برای APK دیباگ
