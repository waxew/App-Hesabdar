# Changelog

تمام تغییرات مهم پروژه حسابدار در این فایل ثبت می‌شوند.

## 1.0.0

- ارتقای `versionName` به `1.0.0` و `versionCode` به `7`.
- ارتقای Room Schema از 5 به 6 با Migration غیرمخرب `5 -> 6`.
- اضافه شدن `sourceType/sourceId` به گردش خزانه برای ردیابی منبع تراکنش و Reversal قابل اتکا.
- اتصال تسویه فاکتور به صندوق/بانک؛ تسویه دیگر فقط در جدول پرداخت ثبت نمی‌شود و مانده خزانه را نیز تغییر می‌دهد.
- سند حسابداری تسویه بر اساس نوع حساب خزانه به حساب سیستمی «صندوق» یا «بانک» ارسال می‌شود.
- ابطال فاکتور، وجه تسویه‌شده را در همان خزانه معکوس می‌کند؛ برای اسناد قدیمی Beta امکان انتخاب خزانه جایگزین وجود دارد.
- عملیات دریافت/پرداخت/درآمد/هزینه بدون انتخاب حساب خزانه رد می‌شوند.
- اعتبارسنجی حساب خزانه فعال قبل از ثبت عملیات مالی.
- ثبت موجودی افتتاحیه کالا در کارتکس با نوع `OPENING` و Audit Trail.
- جلوگیری از تغییر نوع کالا/خدمت پس از ایجاد برای حفظ تاریخچه انبار.
- جلوگیری از ایجاد اقساط صفر و جلوگیری از پرداخت بیش از مانده قسط.
- اضافه شدن `InstallmentMath` و تست‌های تقسیم مبلغ اقساط.
- اضافه شدن Backup قابل‌انتقال رمزگذاری‌شده HDBX با PBKDF2-HMAC-SHA256 و AES-GCM.
- کنترل Header SQLite و ایجاد Backup اضطراری قبل از Restore رمزگذاری‌شده.
- اضافه شدن تست بردار استاندارد PBKDF2 و تشخیص SQLite Header.
- اصلاح رفتار Back در Drawer، صفحات اصلی، پروفایل طرف‌حساب و جزئیات/کارتکس کالا.
- GitHub Actions ابتدا Unit Test و سپس Debug APK را می‌سازد.
- آماده‌سازی Build Release امضاشده فقط از طریق GitHub Secrets؛ هیچ Keystore یا رمز خصوصی در Repo قرار نمی‌گیرد.
- Artifact تستی نسخه پایدار: `App-Hesabdar-v1.0.0-debug`.

## 0.11.0 Beta

- شماره سند پایدار و یکتا.
- جدول شمارنده اسناد.
- وضعیت‌های `POSTED`، `VOIDED` و `REVERSAL`.
- ابطال فروش و خرید با سند معکوس و علت اجباری.
- Snapshot بهای واحد ردیف برای گزارش تاریخی سود.
- اصلاح نمایش نسخه از `BuildConfig`.

## 0.9.0 Beta

- Drawer حرفه‌ای RTL و پروفایل محلی.
- حسابداری دوبل خودکار و دستی.
- تراز آزمایشی و گزارش سود برآوردی.
- Backup/Restore، PIN، PDF/CSV، تاریخ شمسی و یادآوری آفلاین.

## 0.3.0

- صندوق و حساب بانکی.
- دریافت/پرداخت مستقل.
- درآمد و هزینه.
- کدینگ حسابداری و اسناد دوبل.
- چک و اقساط.
- Audit Trail.

## 0.2.0

- فاکتور، ردیف فاکتور، پرداخت و کارتکس.
- فروش/خرید Atomic با Transaction.
- افزایش/کاهش خودکار موجودی.
- داشبورد فروش، خرید، مطالبات و بدهی.

## 0.1.0

- اسکلت Kotlin + Compose + Material 3.
- Room + SQLite محلی.
- رابط فارسی و RTL.
- اشخاص، کالاها و داشبورد اولیه.
